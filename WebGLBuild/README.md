# Run WebGL application in local environment

1. Enter the following command.
    ```
    $ deno run --allow-net --allow-read=. index.ts
    ```
1. Open in browser.
    ```
    http://localhost:3333/
    ```
